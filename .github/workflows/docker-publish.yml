name: Build and Push Docker Images

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - 'backend/**'
      - 'docker-compose*.yml'
      - '.github/workflows/docker-publish.yml'
  workflow_dispatch:

env:
  DOCKERHUB_USERNAME: kevin950805

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform:
          - linux/arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract platform info
        id: platform
        run: |
          platform=${{ matrix.platform }}
          echo "pair=${platform//\//-}" >> $GITHUB_OUTPUT

      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: ${{ env.DOCKERHUB_USERNAME }}/ebook-reader-frontend:latest-${{ steps.platform.outputs.pair }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: ${{ env.DOCKERHUB_USERNAME }}/ebook-reader-backend:latest-${{ steps.platform.outputs.pair }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

  merge-manifests:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create and push Frontend manifest
        run: |
          docker buildx imagetools create -t ${{ env.DOCKERHUB_USERNAME }}/ebook-reader-frontend:latest \
            ${{ env.DOCKERHUB_USERNAME }}/ebook-reader-frontend:latest-linux-arm64

      - name: Create and push Backend manifest
        run: |
          docker buildx imagetools create -t ${{ env.DOCKERHUB_USERNAME }}/ebook-reader-backend:latest \
            ${{ env.DOCKERHUB_USERNAME }}/ebook-reader-backend:latest-linux-arm64

      - name: Summary
        run: |
          echo "## Docker Images Published" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Frontend: \`${{ env.DOCKERHUB_USERNAME }}/ebook-reader-frontend:latest\`" >> $GITHUB_STEP_SUMMARY
          echo "- Backend: \`${{ env.DOCKERHUB_USERNAME }}/ebook-reader-backend:latest\`" >> $GITHUB_STEP_SUMMARY
